import { PumpGame } from "@/components/pump/PumpGame";

export const metadata = {
  title: "Pump — Balloon Crash",
  description: "Inflate the balloon, ride the multiplier, cash out before it pops!",
};

export default function PumpPage() {
  return <PumpGame />;
}
